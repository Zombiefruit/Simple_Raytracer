Kieran Williams
kieran.williams@queensu.ca	
10025408

One issue I have is with the Vertex array called vests in the triangle object. In order for PartB to work, it must be placed in public, and in order for PartA to work it must be private. I built a small getVerts function to return the array but I had issues getting it to work. 